<div class= "container w-50 mx-auto m-20">
  <h1>Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
  <p class= "link "><%= link_to "Create a New Task", new_task_path%></p>
</div>
